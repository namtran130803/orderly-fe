services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_DOCKER=true
      - VITE_PROXY_TARGET=http://backend:3000
      - VITE_API_URL=/api
    networks:
      - orderly-network

networks:
  orderly-network:
    name: orderly-network
