# Relative URL: works from any device on LAN (phone uses 192.168.x.x:5173 → Vite proxies /api → backend).
# For production, override with full API origin in .env.production if needed.
VITE_API_URL=/api
