services:
  frontend:
    image: orderly-fe:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-80}:80"
    networks:
      - orderly-network
    restart: unless-stopped

networks:
  orderly-network:
    name: orderly-network
